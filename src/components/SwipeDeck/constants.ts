/** Default fraction of screen width to commit a swipe. */
export const SWIPE_THRESHOLD = 0.25;

/** Default number of cards stacked behind the top card. */
export const STACK_SIZE = 3;

/** Duration (ms) of the fling-out animation after release. */
export const SWIPE_OUT_DURATION = 250;
